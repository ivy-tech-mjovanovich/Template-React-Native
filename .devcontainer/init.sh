## All of these commands were run to create this template:
# sudo npm install create-expo-app --global
# sudo npx create-expo-app -t expo-template-blank-typescript ReactNativeApp
# cd ReactNativeApp
# sudo npx expo install react-native-web react-dom @expo/metro-runtime

## Now that the app is set up, we just have to run the following:
cd ReactNativeApp
sudo npm install;

## To clone without the whole commit history:
# git clone https://github.com/ivy-tech-mjovanovich/Template-React-Native.git --depth 1